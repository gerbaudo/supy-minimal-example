git clone git://github.com/<user>/supy-minimal-example  # clone repo: <user> can be davidegerbaudo or a forking user
cd supy-minimal-example
git clone git://github.com/elaird/supy                  # checkout supy dependence
export PYTHONPATH=$PYTHONPATH:`pwd`                     # add directory containing supy to your python path
export PATH=$PATH:`pwd`/supy/run                        # optionally add to your path
supy example_analyses.py --loop 1                       # run the example (the example input files are located on AFS)
